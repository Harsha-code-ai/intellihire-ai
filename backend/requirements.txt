# ─── IntelliHire Pro — Python dependencies ────────────────────────────────────
#
# KEY FIX: bcrypt must be pinned to 4.0.1 OR the passlib bcrypt monkeypatch
# in security.py must be active.  We pin to 4.1.3 (latest stable as of 2025)
# AND keep the monkeypatch so either version works.
#
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic[email]==2.7.1
python-multipart==0.0.9
python-dotenv==1.0.1

# Database
SQLAlchemy==2.0.30
psycopg2-binary==2.9.9

# Auth
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
python-jose[cryptography]==3.3.0

# AI — pinned together to avoid proxies/timeout conflict
openai==1.51.0
httpx==0.27.0

# Resume parsing
pdfplumber==0.11.0
pypdf==4.2.0
python-docx==1.1.2

# Utilities
aiofiles==23.2.1